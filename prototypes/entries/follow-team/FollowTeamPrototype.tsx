'use client';

// Duplicate of the team-profile prototype, surfaced as its own "follow-team"
// entry. Re-exports the same component so the two stay in sync.
export { default } from '../team-profile/TeamProfilePrototype';
