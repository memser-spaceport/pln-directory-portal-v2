// Required by Next.js for parallel routes — serves as a fallback when this slot
// has no matching page for the current URL during client-side navigation.
export default function Default() {
  return null;
}
