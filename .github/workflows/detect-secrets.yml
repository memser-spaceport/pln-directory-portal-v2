# name: Detect Secrets

# on:
#   push:
#     branches:
#       - "feat/setup"
#   pull_request:
#     branches:
#     - "develop"
#     - "staging"
#     - "main"

# jobs:
#   detect-secrets:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.10'

#       - name: Install Detect-Secrets
#         run: |
#           pip install detect-secrets

#       - name: Run Detect-Secrets
#         run: |
#           detect-secrets scan
#           detect-secrets scan --baseline .secrets.baseline || (echo "Secrets detected! Failing the workflow." && exit 1)

# name: Secretlint
# on: [push, pull_request]
# permissions:
#   contents: read
# jobs:
#   test:
#     name: "Secretlint"
#     runs-on: ubuntu-latest
#     steps:
#       - name: checkout
#         uses: actions/checkout@v3
#       - name: setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 20
#       - name: Install
#         run: npm ci
#       - name: Lint with Secretlint
#         run: npx secretlint "**/*"
name: Secretlint on Changed Files

on: [push, pull_request]

jobs:
  detect-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install secretlint
        run: npm install --save-dev secretlint

      - name: Run secretlint
        run: npx secretlint "**/*"