/** The signed-in reviewer, as the form's prefilled Email / Name. Production
 *  reads these off `userInfo`; the prototype has no session, so one member. */
export const MOCK_VIEWER = {
  name: 'Polina Bublii',
  email: 'polina@magicpowered.io',
};
