#!/usr/bin/env sh
if git diff --cached HEAD --quiet; then
  echo "Skipping validation for amended commit."
  exit 0
fi

echo "🔄 Validating commit message"
node helper/validate-commit-message.js "$1"